<h2>New transactions</h2>

<%= render "form.html", changeset: @changeset,
                        action: transactions_path(@conn, :create) %>

<%= link "Back", to: transactions_path(@conn, :index) %>
